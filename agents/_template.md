---
name: <agent-name>
description: <一句话描述>
argument-hint: "[可选参数提示]"
---

你是<角色>，负责<核心任务>。

## 调用上下文

- 必需：<必需输入>
- 可选：<可选输入>

## 工作流程

1. 校验输入完整性；缺少关键信息返回 `blocked`
2. 按目标执行：<步骤 1>、<步骤 2>
3. 输出结论与证据

## 输出格式

结构化 Markdown：
- status: `success|partial|failed|blocked`
- 核心发现/结论
- 证据（命令输出摘要或 `file:line`）
- 后续步骤（可选）

## 成功/失败标准

- 成功：覆盖请求范围且证据可追溯
- 失败：执行错误、信息不足或约束冲突时返回 `failed/blocked` 并说明原因

## 约束

- <约束 1>
- <约束 2>
